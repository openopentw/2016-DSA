#include <cstdio>#include <string>using std::string;/*const int NCHAR = 8000000;int main(){	int max = 0;	char buf[NCHAR];		FILE *fp = fopen("/tmp2/handsomeP/5gm.small.txt", "rb");	while(int fend = fread(buf, sizeof(char), NCHAR, fp)) {		int len = 0;		for(int i = 0; i < NCHAR; ++i) {			if(buf[i] != '\n') {				++len;			} else {				max = len > max? len : max;				len = 0;			}		}	}	fclose(fp);		printf("%d\n", max);	return 0;}*/int main(){	string str;	if(str)	puts("Yee");	return 0;}